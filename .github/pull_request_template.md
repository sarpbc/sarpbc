## Summary

Briefly describe what this PR changes and why.

## Checklist

- [ ] `pnpm lint` / `pnpm fmt:check` pass locally
- [ ] Relevant tests updated or added (`pnpm --filter back test` when touching the API)
- [ ] User-facing strings use i18n (`en-US` + `fr-FR`) when applicable
- [ ] No secrets or `.env` files included
- [ ] Linked to an issue when relevant

## Test plan

- [ ] 
